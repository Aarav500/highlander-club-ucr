{
  "name": "fullstack-template",
  "version": "10.0.0",
  "private": true,
  "description": "FullStack Template — V10.0 Total Perfection: Hyper-personalization, 50% bug reduction, 100+ micro-agents, digital twins, AI literacy, developer happiness, 75% cost savings, ethics compliance, trend radar, 3× productivity",
  "scripts": {
    "dev": "echo 'Run dev servers from apps/<app-name>/ — see README.md'",
    "lint": "echo 'Run lint from apps/<app-name>/ — see .agent/capabilities/code-audit.md'",
    "test": "echo 'Run tests from apps/<app-name>/ — see .agent/workflows/code-audit-fix.md'"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
